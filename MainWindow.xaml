<Window x:Class="RittalMåling.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RittalMåling"
        Title="RittalMåling" Height="700" Width="1300"
        MinHeight="600" MinWidth="1000"
        Background="#FF1A2526"
        FontFamily="Inter, Segoe UI" FontSize="14">
    <Window.Resources>
        <!-- Define modern color brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF00B294"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFE6ECEC"/>
        <SolidColorBrush x:Key="PanelBackground" Color="#2A3B3C"/>
        <!-- Style for buttons with modern animations -->
        <Style TargetType="Button" x:Key="RoundedButtonStyle">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF00A88F"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.05" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.05" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF008E7A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF6B6B6B"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>
        <!-- Style for Grid Buttons (no text) -->
        <Style x:Key="GridButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Background" Value="#FF4A5A5B"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Content" Value=""/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5A6A6B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF3A4B4C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style for TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderBrush" Value="#FF4A5A5B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FF2A3B3C"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF3A4B4C"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF0078D4" BlurRadius="6" ShadowDepth="0" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style for Label -->
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <!-- Style for Last Measurement Text -->
        <Style TargetType="TextBlock" x:Key="LastMeasurementStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="8,8,0,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style for Excel Text -->
        <Style TargetType="TextBlock" x:Key="ExcelTextStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style for Selected File TextBlock -->
        <Style TargetType="TextBlock" x:Key="SelectedFileStyle">
            <Setter Property="Foreground" Value="#FFB0B8B8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="MaxWidth" Value="300"/>
        </Style>
    </Window.Resources>
    <Border Background="{StaticResource PanelBackground}" CornerRadius="16" Margin="20" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Last Measurement -->
            <Grid Grid.Row="0">
                <TextBlock x:Name="LastMeasurementText" Text="{Binding LastMeasurement}" Style="{StaticResource LastMeasurementStyle}"/>
            </Grid>
            <!-- Input Controls -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Sections:" ToolTip="Number of grid sections" Grid.Column="0"/>
                <TextBox x:Name="SectionBox" Text="{Binding Sections, UpdateSourceTrigger=PropertyChanged}" ToolTip="Enter number of sections (1-10)" Grid.Column="1"/>
                <Label Content="Rows:" ToolTip="Number of rows per section" Grid.Column="2"/>
                <TextBox x:Name="RowBox" Text="{Binding Rows, UpdateSourceTrigger=PropertyChanged}" ToolTip="Enter number of rows (1-20)" Grid.Column="3"/>
                <Label Content="Columns:" ToolTip="Number of columns per section" Grid.Column="4"/>
                <TextBox x:Name="ColBox" Text="{Binding Cols, UpdateSourceTrigger=PropertyChanged}" ToolTip="Enter number of columns (1-10)" Grid.Column="5"/>
                <Button Content="Rebuild Grid" Click="Rebuild_Click" Width="140" Style="{StaticResource RoundedButtonStyle}" ToolTip="Rebuild the grid with specified dimensions" Grid.Column="7"/>
                <Button Content="Select Excel File" Click="SelectExcelFile_Click" Width="140" Style="{StaticResource RoundedButtonStyle}" ToolTip="Choose an Excel file to log measurements" Grid.Column="9"/>
                <Button Content="Delete Last" Click="DeleteLast_Click" Width="140" Style="{StaticResource RoundedButtonStyle}" ToolTip="Remove the last measurement from the Excel file" Grid.Column="11"/>
                <TextBlock x:Name="SelectedFileText" Text="{Binding SelectedExcelFile}" Style="{StaticResource SelectedFileStyle}" ToolTip="Currently selected Excel file for logging" Grid.Column="12"/>
            </Grid>
            <!-- Custom Grid Layout Controls -->
            <Grid Grid.Row="1" Margin="0,40,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Custom Layout:" ToolTip="Define custom grid layout" Grid.Column="0"/>
                <Button Content="Edit Grid Layout" Click="EditGridLayout_Click" Width="140" Style="{StaticResource RoundedButtonStyle}" ToolTip="Open grid layout editor" Grid.Column="1"/>
            </Grid>
            <!-- Grid Display -->
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Grid.Row="2" Margin="0,20,0,0">
                <Border Background="#FF2A3B3C" CornerRadius="10" Padding="10">
                    <DockPanel>
                        <StackPanel x:Name="MainPanel" Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </DockPanel>
                </Border>
            </ScrollViewer>
            <!-- Results -->
            <StackPanel Grid.Row="3" Margin="0,20,0,0" Orientation="Vertical" HorizontalAlignment="Left">
                <TextBlock x:Name="ExcelText" Text="{Binding ExcelDisplayText}" Style="{StaticResource ExcelTextStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>